import { BodyTDirective } from './body-t.directive';

describe('BodyTDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyTDirective();
    expect(directive).toBeTruthy();
  });
});
